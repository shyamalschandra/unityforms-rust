[package]
name = "unityform-windows"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Compatibility re-export; use `unityform-platform` for cross-platform shell support"
publish = false

[dependencies]
unityform-platform = { path = "../unityform-platform" }
